# Backend contract reconciliation — COMPLETE

The canonical backend contract lifecycle model was fully reconciled from the
obsolete "proposed/unmerged/undeployed" state to the RELEASED (merged + deployed
+ live-verified) state. The generator was updated so the fully-merged state is a
first-class lifecycle, not a fake proposed layer.

## Changes
- `scripts/build-contract-snapshot.mjs`: provenance → backendCommit/mergedMainCommit/
  baseCommitInspected `72e6e58`, migrationCount 69, finalMigration
  `20260908000000_slab_permission_model`, mergeState `merged` (+mergeCommit),
  deploymentState `deployed` (+deployedCommit), liveVerificationState
  `passed-production` (+stagingVerifiedAt). Released state made first-class:
  `proposedMigrationCount == migrationCount` is valid ONLY when promoted (empty
  pending layer); the old strict `proposed > merged` invariant now only applies
  while unpromoted. Canonical mode publishes formerly-gated operations once released.
- `contracts/backend-operations.ts`: CONTRACT_VERSION → `1.3.0-merged-72e6e58d-m69-proposed-m69`;
  saveConfirmedSlabFromAnalysis + startSlabAnalysis → `READY` (notes updated).
- `contracts/proposed/PROPOSED_STATE.json`: all merged/proposed/deploy/verify fields
  → released reality; operationStatus → READY.
- `contracts/canonical/backend-capabilities.json` + `contracts/proposed/backend-capabilities.proposed.json`:
  regenerated by the authoritative generator.
- `src/test/analyze/analysis-contract.test.ts`: assertions updated to the released state.

## Verification
- Validator: `node scripts/validate-contracts.mjs` → OK, 36 operations, version 1.3.0-merged-72e6e58d-m69-proposed-m69.
- Determinism: two consecutive `--mode canonical` runs produced identical sha256 (33b0c791…).
- Tests: analysis-contract 31/31 pass; full backend vitest 1441 pass / 162 skipped.
  (Only non-pass = pre-existing local `pg` devDependency import in the env-gated
  atomic-confirmed-save integration test; identical on origin/main; `npm ci` in CI resolves it.)
- Operation READY states each verified against LIVE production (see backend-contract-manifest.json).
- No stale `d8088f2a` / `ba3953fd` provenance in the active (canonical) artifact. The top-level
  `contracts/backend-capabilities.json` is the documented pre-separation legacy artifact,
  retained unchanged by design (not the manifest mirror; not consumed by the frontend).

## Frontend sync
- `bun scripts/build-contract-snapshot.mjs --mode canonical --frontend <slab-scribe-pro>` re-emitted
  the vendored snapshot at `src/integrations/backend/generated/` (CONTRACT_VERSION=1.3.0-merged-72e6e58d-m69-proposed-m69,
  BACKEND_COMMIT=72e6e58, MIGRATION_COUNT=69, MERGE_STATE=merged, DEPLOYMENT_STATE=deployed).
