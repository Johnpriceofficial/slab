node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local
.DS_Store
*.log
npm-debug.log*
# This is a bun project (bun.lock). Ignore any stray npm lockfile so an
# accidental `npm install` can't pollute the tree or break CI's frozen install.
package-lock.json
supabase/.temp
.vscode/*
!.vscode/extensions.json

# Benchmark run outputs (raw model responses, reports)
benchmark-results/
